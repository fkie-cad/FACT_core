[project]
name = "fact-plugin-users_and_passwords"
version = "1.1.0"
dependencies = [
    "fact-core",
    "common_helper_passwords @ git+https://github.com/fkie-cad/common_helper_passwords.git",
]

[tool.uv.sources]
fact-core = { workspace = true }
