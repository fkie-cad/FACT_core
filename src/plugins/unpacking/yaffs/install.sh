#!/usr/bin/env bash

cd "$( dirname "${BASH_SOURCE[0]}" )"

echo "------------------------------------"
echo "          install unyaffs           "
echo "------------------------------------"

sudo apt-get install -y unyaffs

exit 0
