#!/usr/bin/env bash

cd "$( dirname "${BASH_SOURCE[0]}" )"

echo "------------------------------------"
echo "           install kpartx           "
echo "------------------------------------"

sudo apt-get install -y kpartx

exit 0
