version: '3'

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
    web:
        build:
            context: ./nginx
            dockerfile: Dockerfile
        restart: always
        ports:
        - 8000:8000
