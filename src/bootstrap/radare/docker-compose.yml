version: '2'

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
    server:
        build:
            context: ./nginx
            dockerfile: Dockerfile
        restart: always
        ports:
        - 8000:8000
